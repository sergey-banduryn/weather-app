import type { ComponentStyles } from '@types';

const styles = {
  box: {
    alignItems: 'center',
    display: 'flex',
    height: 318,
    justifyContent: 'center',
  },
} satisfies ComponentStyles;

export { styles };
