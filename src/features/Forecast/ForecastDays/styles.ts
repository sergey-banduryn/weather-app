import type { ComponentStyles } from '@types';

const styles = {
  box: {
    display: 'flex',
    gap: 1,
    marginBottom: 2,
    overflowX: 'auto',
  },
} satisfies ComponentStyles;

export { styles };
