import type { ComponentStyles } from '@types';

const styles = {
  box: {
    alignItems: 'center',
    display: 'flex',
    flexWrap: 'wrap',
    gap: 1,
    minHeight: 30,
    my: 1,
  },
} satisfies ComponentStyles;

export { styles };
