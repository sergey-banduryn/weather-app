const styles = {
  image: {
    borderRadius: 16,
    bottom: 0,
    height: '100%',
    left: 0,
    objectFit: 'cover',
    opacity: 0.2,
    pointerEvents: 'none',
    position: 'absolute',
    right: 0,
    top: 0,
    width: '100%',
    zIndex: 1,
  },
} as const;

export { styles };
